# scripts/python/requirements.txt  (runtime + tooling in one place)
dbt-core==1.7.*                   # keep in lockstep with dbt-snowflake
dbt-snowflake==1.7.*              # adapter for Snowflake

snowflake-connector-python[pandas]==3.8.*  # faster pandas fetches
pandas==2.2.*                     # 2.x pin for stability

markdown==3.6.*                   # for lightweight HTML dashboards
jinja2==3.1.*                     # dbt/templates
python-dateutil==2.9.*
pyyaml==6.0.*
tabulate==0.9.*

faker==25.*                       # synthetic data for seeds/demos

# Linting/Dev tools (consider moving to requirements-dev.txt)
sqlfluff==3.0.3
sqlfluff-templater-dbt==3.0.3
pre-commit==3.7.*
