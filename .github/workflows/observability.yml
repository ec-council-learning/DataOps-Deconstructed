name: Observability Metrics (dbt Monitoring)

# Builds first-class dbt "monitoring" models and publishes a unified Pages site.
# Publishes per-environment under: /<env>/latest and archives runs at: /<env>/runs/<RUN_ID>/

on:
  workflow_call:
    inputs:
      environment:
        description: "Target environment (dev/prod/ci_cd)"
        required: false
        type: string
      extra_flags:
        description: "Optional flags/selectors for dbt"
        required: false
        type: string
      requirements_path:
        description: "Path to Python requirements file (for Snowflake connector, etc.)"
        required: false
        type: string
    secrets:
      SNOWFLAKE_ACCOUNT: {required: false}
      SNOWFLAKE_USER: {required: false}
      SNOWFLAKE_PASSWORD: {required: false}
      SNOWFLAKE_ROLE: {required: false}
      SNOWFLAKE_WAREHOUSE: {required: false}
      SLACK_WEBHOOK_URL: {required: false}
      SNOWFLAKE_PRIVATE_KEY_B64: {required: false}
      SNOWFLAKE_PRIVATE_KEY_PASSPHRASE: {required: false}

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: observability-${{ github.ref }}
  cancel-in-progress: false

jobs:
  generate_metrics_dashboard:
    runs-on: ubuntu-latest
    env:
      # Snowflake
      SNOWFLAKE_ACCOUNT: ${{ secrets.SNOWFLAKE_ACCOUNT }}
      SNOWFLAKE_USER: ${{ secrets.SNOWFLAKE_USER }}
      SNOWFLAKE_PASSWORD: ${{ secrets.SNOWFLAKE_PASSWORD }}
      SNOWFLAKE_ROLE: ${{ secrets.SNOWFLAKE_ROLE }}
      SNOWFLAKE_WAREHOUSE: ${{ secrets.SNOWFLAKE_WAREHOUSE }}
      SNOWFLAKE_PRIVATE_KEY_B64: ${{ secrets.SNOWFLAKE_PRIVATE_KEY_B64 }}
      SNOWFLAKE_PRIVATE_KEY_PASSPHRASE: ${{ secrets.SNOWFLAKE_PRIVATE_KEY_PASSPHRASE }}

      # Context
      RUN_ID: ${{ github.run_id }}
      RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
      GIT_SHA: ${{ github.sha }}
      DBT_ENVIRONMENT: ${{ inputs.environment || 'prod' }}
      DBT_PROFILES_DIR: ${{ github.workspace }}/scripts/dbt
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
      TZ: UTC

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: |
            requirements.txt
            **/requirements.txt
            ${{ inputs.requirements_path }}

      - name: Install Python deps (connector, markdown, jq)
        if: inputs.requirements_path != ''
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip
          pip install -r "${{ inputs.requirements_path }}"

      - name: Install dbt packages
        working-directory: scripts/dbt
        shell: bash
        run: |
          set -euo pipefail
          dbt deps

      # Generate dynamic CSV seed files
      - name: Generate Dynamic CSV Seed Files
        run: |
          python scripts/python/create_seed.py

      # Build only the observability models using the new tags.  The `observability` tag
      # selects the monitoring layer defined in schema.yml.  The `+` suffix ensures
      # upstream dependencies (e.g., silver/gold) are built as needed.  Extra flags
      # from the caller are appended verbatim.
      - name: Build monitoring models
        working-directory: scripts/dbt
        shell: bash
        run: |
          set -euo pipefail
          test -f profiles.yml
          # Append `|| true` so test failures in observability models do not fail the pipeline.
          dbt build --select tag:observability+ ${{ inputs.extra_flags }} --target "${DBT_ENVIRONMENT}" || true

      - name: Bronze checks
        working-directory: scripts/dbt
        run: dbt test --select tag:bronze ${{ inputs.extra_flags }} --target "${DBT_ENVIRONMENT}" || true

      - name: Silver checks
        working-directory: scripts/dbt
        run: dbt test --select tag:silver ${{ inputs.extra_flags }} --target "${DBT_ENVIRONMENT}" || true

      - name: Gold checks
        working-directory: scripts/dbt
        run: dbt test --select tag:gold ${{ inputs.extra_flags }} --target "${DBT_ENVIRONMENT}" || true

      - name: Monitoring checks
        working-directory: scripts/dbt
        run: dbt test --select tag:observability ${{ inputs.extra_flags }} --target "${DBT_ENVIRONMENT}" || true

      - name: Slack — Success
        if: success() && env.SLACK_WEBHOOK_URL != ''
        uses: slackapi/slack-github-action@v1.26.0
        env:
          SLACK_WEBHOOK_URL: ${{ env.SLACK_WEBHOOK_URL }}
          SLACK_WEBHOOK_TYPE: INCOMING_WEBHOOK
        with:
          payload: |
            {
              "blocks": [
                { "type": "header", "text": { "type": "plain_text", "text": "✅ Observability (dbt monitoring) published" } },
                { "type": "section", "fields": [
                  { "type":"mrkdwn", "text":"*Run:* <${{ env.RUN_URL }}|${{ env.RUN_ID }}>{{}}" },
                  { "type":"mrkdwn", "text":"*Commit:* `${{ env.GIT_SHA }}`" }
                ]}
              ]
            }

      - name: Slack — Failure
        if: failure() && env.SLACK_WEBHOOK_URL != ''
        uses: slackapi/slack-github-action@v1.26.0
        env:
          SLACK_WEBHOOK_URL: ${{ env.SLACK_WEBHOOK_URL }}
          SLACK_WEBHOOK_TYPE: INCOMING_WEBHOOK
        with:
          payload: |
            { "text": "🚨 Observability (dbt monitoring) failed. See ${{ env.RUN_URL }}." }
